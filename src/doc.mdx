---
route: /
name: Dou
---

import {Playground, PropsTable} from 'docz';
import {DouProvider, DouFunctionsConsumer, Dou} from '.';
import en from './locales/en';

## 日本語

<Playground>
	{() => {
		const callback = buttonIndex => {
			if (buttonIndex === 0) {
				console.log('primary button clicked');
			} else {
				console.log('normal button clicked');
			}
		}

		return (
			<DouProvider callback={callback}>
				<DouFunctionsConsumer>
					{({ask}) => {
						return (
							<>
								<button onClick={ask('ハンバーガー食べますか？')}>ハンバーガー食べますか？</button>
								<Dou />
							</>
						)
					}}
				</DouFunctionsConsumer>
			</DouProvider>
		)
	}}
</Playground>

##　English

<Playground>
	{() => {
		const callback = buttonIndex => {
			if (buttonIndex === 0) {
				console.log('primary button clicked');
			} else {
				console.log('normal button clicked');
			}
		}

		return (
			<DouProvider callback={callback}>
				<DouFunctionsConsumer>
					{({ask}) => {
						return (
							<>
								<button onClick={ask('Eat hamburgers?')}>Eat hamburgers?</button>
								<Dou locale={en} />
							</>
						)
					}}
				</DouFunctionsConsumer>
			</DouProvider>
		)
	}}
</Playground>
